# i18n (Internationalization)

> 원본: https://ironact.gitbook.io/ironact-docs/CrEhRPJQJpia3xh9iqbi/knowledge-fe/i18n

---

## 1. Next.js i18n 설정

```ts
// next.config.ts
export default {
  i18n: {
    locales: ['ko', 'en', 'ja'],
    defaultLocale: 'ko',
    localeDetection: true,  // Accept-Language 헤더 자동 감지
  },
}
```

---

## 2. next-intl 설정 (권장 라이브러리)

```
npm install next-intl
```

```
messages/
  ko.json
  en.json
  ja.json

app/
  [locale]/
    layout.tsx
    page.tsx
```

```json
// messages/ko.json
{
  "common": {
    "save": "저장",
    "cancel": "취소",
    "delete": "삭제"
  },
  "auth": {
    "login": "로그인",
    "logout": "로그아웃",
    "welcome": "{name}님, 환영합니다!"
  },
  "errors": {
    "required": "필수 입력 항목입니다",
    "email": "유효한 이메일을 입력하세요"
  }
}
```

```ts
// i18n/request.ts
import { getRequestConfig } from 'next-intl/server'

export default getRequestConfig(async ({ locale }) => ({
  messages: (await import(`../messages/${locale}.json`)).default,
}))
```

```tsx
// app/[locale]/layout.tsx
import { NextIntlClientProvider } from 'next-intl'
import { getMessages } from 'next-intl/server'

export default async function LocaleLayout({
  children,
  params: { locale },
}: {
  children: ReactNode
  params: { locale: string }
}) {
  const messages = await getMessages()

  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

---

## 3. 번역 사용

```tsx
// Server Component
import { useTranslations } from 'next-intl'

export default function HomePage() {
  const t = useTranslations('auth')

  return (
    <div>
      <h1>{t('welcome', { name: '홍길동' })}</h1>
      <button>{t('login')}</button>
    </div>
  )
}

// Client Component
'use client'
import { useTranslations } from 'next-intl'

export function LoginButton() {
  const t = useTranslations('auth')
  return <button>{t('login')}</button>
}

// 복수형 처리
// messages/ko.json
// "items": "{count, plural, =0 {항목 없음} one {항목 1개} other {항목 {count}개}}"
t('items', { count: 3 })  // → "항목 3개"
```

---

## 4. 날짜/숫자/통화 포맷

```tsx
import { useFormatter } from 'next-intl'

function PriceDisplay({ price, date }: { price: number; date: Date }) {
  const format = useFormatter()

  return (
    <div>
      {/* 통화 */}
      <span>{format.number(price, { style: 'currency', currency: 'KRW' })}</span>
      {/* → ₩1,000 (ko) / ¥1,000 (ja) */}

      {/* 날짜 */}
      <span>{format.dateTime(date, { dateStyle: 'long' })}</span>
      {/* → 2024년 1월 15일 (ko) / January 15, 2024 (en) */}

      {/* 상대 시간 */}
      <span>{format.relativeTime(date)}</span>
      {/* → 3일 전 (ko) / 3 days ago (en) */}
    </div>
  )
}
```

---

## 5. 언어 전환

```tsx
'use client'
import { useRouter, usePathname } from 'next/navigation'
import { useLocale } from 'next-intl'

export function LocaleSwitcher() {
  const router = useRouter()
  const pathname = usePathname()
  const currentLocale = useLocale()

  const locales = [
    { code: 'ko', label: '한국어' },
    { code: 'en', label: 'English' },
    { code: 'ja', label: '日本語' },
  ]

  function switchLocale(locale: string) {
    // /ko/dashboard → /en/dashboard
    const newPath = pathname.replace(`/${currentLocale}`, `/${locale}`)
    router.push(newPath)
  }

  return (
    <select value={currentLocale} onChange={(e) => switchLocale(e.target.value)}>
      {locales.map(({ code, label }) => (
        <option key={code} value={code}>{label}</option>
      ))}
    </select>
  )
}
```

---

## 6. RTL (Right-to-Left) 지원

```tsx
// 아랍어, 히브리어 등 RTL 언어 지원
const rtlLocales = ['ar', 'he']

export default function Layout({ params: { locale } }: Props) {
  const dir = rtlLocales.includes(locale) ? 'rtl' : 'ltr'

  return (
    <html lang={locale} dir={dir}>
      <body>{children}</body>
    </html>
  )
}
```

```css
/* Tailwind RTL 지원 */
.ml-4 {
  /* LTR: margin-left, RTL: margin-right */
}
/* Tailwind v3 logical properties */
.ms-4  /* margin-inline-start — LTR: left, RTL: right */
.me-4  /* margin-inline-end */
.ps-4  /* padding-inline-start */
```

---

## 7. 안티패턴

- **하드코딩된 한국어 텍스트**: 모든 사용자 표시 텍스트는 번역 파일로
- **날짜/숫자 직접 포맷**: `toLocaleString()` 또는 `Intl` API 사용
- **번역 키 중복/누락**: 타입 체크 or 린트 룰로 검출
- **이미지에 텍스트 포함**: 번역 불가 — CSS 오버레이로
- **고정 레이아웃**: RTL 언어에서 깨짐 → logical properties 사용

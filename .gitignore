# 캐시/로그/세션 데이터 (변경 추적 불필요)
projects/
plugins/
cache/
file-history/
telemetry/
transcripts/
shell-snapshots/
session-env/
paste-cache/
todos/
tasks/
sessions/
debug/
statsig/
ide/
lancedb/
.obsidian/
.idea/
workspace-root/
intent/

# 큰 단발성 파일
history.jsonl
*.log
.DS_Store

# Claude Code 런타임 자동 관리 캐시
mcp-needs-auth-cache.json
policy-limits.json

# 백업 디렉토리 자체는 추적 안함 (별도 보관)
backups/

# 빌드 산출물
agents/.last-build

# 백업 파일 (수동 관리)
*.bak
*.bak-*
*.bak.*
*.backup-*
workflows/*.bak-*

# Python 캐시
__pycache__/
*.pyc

# 임시/스냅샷 디렉토리
agents/_pre_symlink_backup/
teams/

# 자동 갱신 캐시
stats-cache.json

# 자기반성 텔레메트리 (프로젝트별 자동 생성, 매번 diff 큼)
self-model/

# 다른 프로젝트의 plan 산출물이 모이는 곳
plans/

# 빈/외부 MCP 등록 파일 (런타임 관리)
mcp.json

# cleanup 마커 (timestamp 자동 갱신)
.last-cleanup

# 자동 인덱싱 상태
skills/index-rag/.index-state.json

# 사내 정보 (private repo 라도 분리)
identity-hub/
debugging-guides/

# 붙여넣은 이미지 임시 캐시 (Claude Code 런타임)
image-cache/
